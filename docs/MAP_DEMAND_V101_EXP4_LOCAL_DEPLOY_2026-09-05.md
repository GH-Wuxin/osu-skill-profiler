# 1.0.1 Exp.4 本地交付与验证

2026-09-05。用户已授权着手修复外部审查，并确认 Control 主值评价最难局部需求。
当前本地评审台为 **1.0.1-experimental.4**，算法选择仍为 `v101-experimental`。

## 已交付

- Control 新增经过阶段核对的 circle 速度向量变化，解决等间距、等节奏时忽略转向的反例。
- 局部聚合为至多八个连续机会、完整源跨度至多三秒，按努力层分别建立支持。
- 方向未知与实际零值分开；全图缺失不否决已观察的正局部需求。
- 评审台显示 Control 局部时间与方向覆盖不完整提示；程序填充跳过证据不足的空值。
- Exp3 的 Flow 完整轴输出、冻结 v100 的其余七轴输出保持不变。
- 问题措辞与盲测候选材料已冻结，模型分数未进入参与页面。

公式、消融和边界见 [Control 实施说明](AIM_CONTROL_VECTOR_EXP4_2026-09-05.md)。

## 最终验证

| 检查 | 结果 |
| --- | --- |
| 完整 unittest | 964 项，961 通过，3 项原有可选语料测试跳过，95.044 秒 |
| 独立 Control 因果/对抗测试 | 32 项通过，包含两项新发现的数值/覆盖边界修复 |
| 正式完整管线对照 | 11/11；Flow 全 payload = Exp3，七轴全 payload = v100；冻结 v100 九轴不变 |
| 已有 BP100，实际 Mods | 100/100 Control 提取与计算完成；全部为部分方向覆盖 |
| UI 脚本验证 | 语法、局部时间、部分/不足证据、空值不填为零通过 |
| 重启选择隔离测试 | 5 项通过；另有 7 个命令行归属与 4 个身份组合检查通过 |
| 在线/离线九轴数值比较 | Lunatic Sky NM、HD；Affection HDHR，共 3/3 一致 |
| 实际无参数重启 | 保留 Exp4，保存选择仍为 v101-experimental |
| 盲测准备检查 | 30 个展示状态及 15 个隔离包装检查通过；模型评分 0，人类回答 0 |

全量测试日志：`tmp/control-exp4-final-full-tests.log`。回归目录：
`tmp/control-exp4-final-regression/`、`tmp/control-exp4-final-bp100/`。

## 本地运行

入口：[BID 评审台](http://127.0.0.1:8767/)。最终索引数量为 102779。
开始部署时该端口没有运行中的服务，因此实际执行的是启动 Exp4；此前 `.3 → .4`
迁移保护只进行了隔离检查，不把它称作本次真实热升级测试。随后又执行了一次实际
无参数重启，确认新服务版本及持久化选择一致。

后续进程使用绝对 CLI 路径。重启脚本同时检查进程、算法身份及版本；如实验版启动
失败，其恢复路径明确指向冻结 v100，不声称能够凭同一算法键恢复旧实验源码。
本次没有触发恢复分支。

在线已核对的 Control/Flow 数值：

| 谱面 | Control | Flow |
| --- | ---: | ---: |
| Lunatic Sky NM | 7.4297742744 | 4.5392444416 |
| Lunatic Sky HD | 7.4297742744 | 4.5392444416 |
| Affection HDHR | 7.6927297046 | 6.9825722055 |

在线产物与两次启动日志保存在 `tmp/deploy-v101-exp4-20260905/`。最终验证清单
`final-verification.json` 记录模型、Control、UI、重启器和测试的 SHA-256，以及
与已冻结 Exp3 副本逐字节相同的模块清单。主要源码：

- `control_vector_v01.py`: `06fbf07b100d4837e20a4e1e25e9c2463f347ff252383ec149286b2dc5d15006`
- `model_v101_experimental.py`: `b2e735d4d925244a93d5163fb70ac57e74d48e3778d9d6fa6602000959c10085`

## 未完成的实证问题

本轮完成的是修复、工程验证、本地交付和盲测准备。3 组 F01 开发对照与 3 组未评分
真实候选已备于 `tmp/control-direction-blind-v01-ready/PLAN.md`；真实候选仍需既往暴露、
混杂及参与者适配复核，尚未启动独立人类收集。AI 没有代填标签，也没有将这些检查
换算为人类准确率。

滑条方向、起停建模、Reading 长周期、形态不同的尺寸响应及跨轴共同人类标尺仍为
开放问题。当前版本保持 Exp，置信度仍为 LOW。
