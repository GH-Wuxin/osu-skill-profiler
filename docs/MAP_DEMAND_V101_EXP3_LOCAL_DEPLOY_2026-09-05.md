# 1.0.1 Exp 本地启用记录 · 2026-09-05

用户在完成 Lunatic Sky 分离识别验证后，明确要求将当前实现投入 **1.0.1 Exp**。本次启用当前 `1.0.1-experimental.3`，没有继续调整评分或识别参数。

## 启用状态

- 本地评审台：`http://127.0.0.1:8767/`。
- 当前算法：`MAP_DEMAND_V101_EXPERIMENTAL`。
- 当前版本：`1.0.1-experimental.3`，阶段 `EXPERIMENTAL`。
- 索引谱面：102779。
- 原运行版本：`0.10.0-beta.9.2`。
- `tmp/runtime-release.json` 已保存 `v101-experimental`；当前进程、用户和机器均无 `SKILL_PROFILER_ALGORITHM` 环境覆盖。

重启工具更新了 Exp 版本校验，并让不带 `-Algorithm` 的普通重启保留已保存的选择。切换成功后再保存选择；启动失败的恢复机制保持可用。全新环境的正式版后备选择仍为 `v100`。

随后实际执行了一次不带算法参数的普通重启，服务成功恢复到同一 `1.0.1-experimental.3`，索引仍为 102779，确认保存选择在重启中生效。

## 在线验证

实际向当前服务发送 `POST /api/analyze`：

| BID | Mod | 返回版本 | 状态 | Flow |
| --- | --- | --- | --- | ---: |
| 931575 | NM | 1.0.1-experimental.3 | OK | 4.539244441598857 |
| 931575 | HD | 1.0.1-experimental.3 | OK | 4.539244441598857 |

两种 Mod 的分数分别与此前固定算法离线结果一致；有效 Mod 正确，分析身份不同。服务每次请求重新计算，版本及 Mod 参与分析身份，不会把旧 beta 结果当成 Exp。

验证工件保存在 `tmp/deploy-v101-exp3-20260905/`，包括服务状态、两份实际响应和检查摘要。已经打开的网页刷新后会加载新版本横幅。

## 当前实验边界

Lunatic Sky 的独立验证仍保留为已知问题：部分带直角小组的分离被 Flow 归属门槛否决，部分组内连接被跨界窗口额外标记。本次用户接受当前状态后启用，没有把它们描述为已修复。取证见 `tmp/flow-reentry-931575/review.md`；绝对尺度仍沿用实验说明中的限制。
